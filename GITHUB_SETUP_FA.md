# راه‌اندازی در GitHub

1. یک Repository جدید بسازید.
2. تمام فایل‌های این پروژه را Push کنید.
3. از `Actions` اجرای `Build Android APK` را انجام دهید.
4. APK ساخته‌شده در بخش Artifacts قابل دریافت است.
5. برای صفحه وب، از `Settings → Pages`، گزینه `Deploy from a branch` و پوشه `/docs` را انتخاب کنید.
6. بعد از ساخت Release، فایل APK را در `releases/ExpertOfficial1405.apk` قرار دهید تا دکمه «دریافت APK» صفحه وب به آن وصل شود.

متن صفحه وب:
- نسخه آزمایشی
- توسعه داده شده توسط صادق مرادزاده
- مرکز کارشناسان رسمی دادگستری لرستان
